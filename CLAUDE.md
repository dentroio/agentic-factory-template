# Claude Code

**Read `PROCESS.md` before starting any implementation task.**

This repo is a tiny demo product for the Agentic Factory. Verify with:

```bash
make ci-local
make run   # http://localhost:8765
```

Do not commit P0–P2 work until a human confirms the running page.

Work orders: `docs/project_management/work_orders/`. Claim files: `docs/factory/runs/`.
